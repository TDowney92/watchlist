<% include ../partials/header.ejs %>

<main class="container">

	<h1>Edit a TV show </h1>



	<h4> TV shows </h4>

    <form method="post">
        <div class="form-group">
            <label for="title" class="col-sm-2 control-label">Title: *</label>
            <input name="title" id="title" required value="<%= watchs.title %>"/>
        </div>
        <div class="form-group">
            <label for="rating" class="col-sm-2 control-label">TV Parental Guide: *</label>
            <input name="rating" id="rating" required value="<%= watchs.rating %>" />
						<select name="rating" id="rating">
							<option<% if (watchs.rating == 'TV-Y') { %> selected }%>>TV-Y</option>
							<option <% if (watchs.rating == 'TV-Y7') { %> selected }%>>TV-Y7</option>
							<option <% if (watchs.rating == 'TV-Y7-FV') { %> selected }%>TV-Y7-FV</option>
							<option <% if (watchs.rating == 'TV-G') { %> selected }%>TV-G</option>
							<option <% if (watchs.rating == 'TV-14') { %> selected }%>TV-14</option>
							<option <% if (watchs.rating == 'TV-M') { %> selected }%>TV-M</option>
						</select>
        </div>
        <div class="form-group">
            <label for="genre" class="col-sm-2 control-label">Genre:</label>
            <select name="genre" id="genre" >
							<option<% if (watchs.genre == 'Drama') { %> selected }%>>Daram</option>
							<option <% if (watchs.genre == 'Comedy') { %> selected }%>>Comedy</option>
							<option <% if (watchs.genre == 'Sci-fi') { %> selected }%>Sci-fi</option>
							<option <% if (watchs.genre == 'Western') { %> selected }%>Western</option>
							<option <% if (watchs.genre == 'Animation') { %> selected }%>Animation</option>
							<option <% if (watchs.genre == 'History') { %> selected }%>History</option>
            </select>
        </div>
        <div class="form-group">
            <label for="seasons" class="col-sm-2 control-label">Number of Seasons:</label>
            <input name="seasons" id="seasons" min="1" type="number" required value="<%= watchs.seasons %>"/>
        </div>
        <button class="btn btn-success col-sm-offset-2">Save</button>
    </form>
</main>

<% include ../partials/footer.ejs %>
